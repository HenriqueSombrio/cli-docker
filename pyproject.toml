[project]
name = "cli-docker"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "questionary",
    "rich"
]

[project.scripts]
mydocker = "cli.main:main_menu"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"